# Server Configuration
server.port=5001

# JSON data file (stored in server-java/data/, separate from Node server)
bus-booking.data-file=data/bookings.json

# CORS - allow React frontend via WebConfig
